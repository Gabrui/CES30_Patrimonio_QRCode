<div class="itens">
  <div class="container">
    <% @itens.each do |i|%> 
      <div class="item">
        <h2><%= i.descricao %></h2>
        <p><%= link_to "Ver detalhes", item_path(i) %></p>
      </div> 
    <% end %>
  </div>
</div>
